After the user selects the add option, there should be a subsequent menu level that gives the user the ability to specify 
their data entry batch by data type.  This will help keep certain data batches together by data type.
